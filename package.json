{
  "name": "ruida-bridge",
  "version": "1.0.0",
  "description": "High-performance TCP-to-UDP relay bridge for Ruida laser controllers",
  "main": "index.ts",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "build": "bun build index.ts --outdir ./dist",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "bun run tsc --noEmit",
    "docker:build": "docker build -f docker/Dockerfile -t ruida-bridge .",
    "docker:run": "docker run -p 5005:5005 --env-file .env ruida-bridge"
  },
  "keywords": ["ruida", "laser", "bridge", "relay", "bun", "tcp", "udp"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/node": "^24.10.7",
    "@typescript-eslint/eslint-plugin": "^8.52.0",
    "@typescript-eslint/parser": "^8.52.0",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "prettier": "^3.7.4"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
