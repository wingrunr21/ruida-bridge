# Ruida Bridge Configuration
# Copy this file to .env and adjust values as needed

# Laser Controller Configuration
LASER_IP=10.0.3.3

# Server Configuration  
BRIDGE_PORT=5005
BRIDGE_HOST=10.0.3.1
HOST=0.0.0.0

# Status Server (uses Bun's native env vars)
PORT=3000

# Optional: Enable debug logging (uncomment to enable)
# DEBUG=1

# Docker-specific overrides (when running in container)
# NODE_ENV=production